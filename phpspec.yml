formatter.name: ~ # pretty, dot, html, progress(default)
suites:
    model_prospect:
        namespace: LIG\Model\Prospect
    model_address:
        namespace: LIG\Model\Address
    model_formation:
        namespace: LIG\Model\Formation
    entity_formation:
        namespace: LIG\Entity\Formation
    model_user:
        namespace: LIG\Model\User
    model_learning:
        namespace: LIG\Model\Learning
    entity_learning:
        namespace: LIG\Entity\Learning
    model_contact:
        namespace: LIG\Model\Contact
    model_lesson:
        namespace: LIG\Model\Lesson
